{
  "name": "@rc/types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo \"(types) no lint yet\"",
    "typecheck": "tsc --noEmit",
    "build": "echo \"(types) source-only package\""
  },
  "devDependencies": { "typescript": "^5.6.2" }
}
